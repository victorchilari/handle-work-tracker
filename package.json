{
	"name": "handle-work-tracker",
	"version": "0.2.0",
	"description": "Server that recive req, open browser local, and do some tasks. In my case server automate my routine with tracking work time",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"start": "npx run index",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@wdio/cli": "^7.6.0",
		"@wdio/local-runner": "^7.6.0",
		"@wdio/mocha-framework": "^7.6.0",
		"@wdio/spec-reporter": "^7.6.0",
		"chromedriver": "^90.0.0",
		"wdio-chromedriver-service": "^7.1.0"
	},
	"author": "Victor Chilari",
	"license": "ISC"
}
